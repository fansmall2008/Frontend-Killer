- 所有子模块共享 pom.xml 中声明的 Spring Boot 3.2 父工程与 Java 17 基线，依赖版本集中管理于 properties 与 dependencyManagement。
- 数据变更统一通过 Flyway 迁移脚本维护，禁止直接修改数据库结构。
- 运行时外部资源（ROM、输出、规则、日志、备份）一律通过 /data 及其子路径挂载，避免硬编码绝对路径。
- 构建产物与发布流程由根级脚本统一管理：build.bat/build-distribution.bat 负责打包，entrypoint.sh 负责容器启动，Dockerfile* 系列提供多环境镜像变体。